name: recorder_keepalive_test
description: Test app for recorder keep-alive performance
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Audio/Video packages
  just_audio: ^0.10.5
  record: ^6.1.2
  video_player: ^2.10.0
  audio_session: ^0.1.21
  speech_to_text: ^7.0.0

  # State management
  get: ^4.6.6

  # Utils
  path_provider: ^2.1.5
  permission_handler: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true